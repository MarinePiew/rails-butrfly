

<h1> Here is the list <h1>

<% @bookings.each do |booking| %>
  <%= booking.au_pair.first_name %>
  <%= booking.au_pair.first_name %>
  <%= booking.au_pair.nationality %>
  <%= booking.au_pair.gender %>
  <%= link_to 'Show', booking, class: 'btn btn-primary' %>
<% end %>
